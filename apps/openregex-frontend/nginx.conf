server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # 1. Serve React Frontend
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 2. Proxy API and Swagger Docs to FastAPI
    # Notice: No trailing slash on '/api' or the backend URL.
    # This ensures '/api' and '/api/info' are passed exactly as written.
    location /api {
        # Resolving the upstream through a variable defers the DNS lookup to
        # request time. With a literal hostname nginx resolves at startup and
        # refuses to boot while the backend container is absent, which turns a
        # slow backend start into a frontend crash loop.
        resolver 127.0.0.11 valid=10s ipv6=off;
        set $backend_upstream http://backend:8000;
        proxy_pass $backend_upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 600s;
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
    }

    # 3. Error Pages
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}